{
  "name": "show-image",
  "version": "4.0.0",
  "description": "Module displays images more easily in Foundry VTT",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "clean": "rimraf dist",
    "zip": "node scripts/zip.js",

    "copy": "npm run copy:templates && npm run copy:styles && npm run copy:languages && npm run copy:module-json",
    "copy:templates": "cpx \"src/app/features/**/*.hbs\" \"dist/templates\"",
    "copy:styles": "cpx \"src/app/features/**/*.css\" \"dist/styles\"",
    "copy:languages": "cpx \"languages/**/*.json\" \"dist/languages\"",
    "copy:module-json": "cpx \"module.json\" \"dist\"",

    "build": "npm run clean && npm run copy && esbuild src/main.ts --bundle --outfile=dist/main.js --format=esm --sourcemap",
    "build:zip": "npm run build && npm run zip"
  },
  "keywords": [],
  "author": "lobowarewolf",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.2.0",
    "cpx": "^1.5.0",
    "esbuild": "^0.25.8",
    "fvtt-types": "github:League-of-Foundry-Developers/foundry-vtt-types#main",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2",
    "archiver": "^5.3.2",
    "javascript-obfuscator": "^4.1.1"
  }
}
